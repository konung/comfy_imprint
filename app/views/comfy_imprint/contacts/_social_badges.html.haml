#social_badges
  - unless ComfyImprint.social_badges.blank?
    - ComfyImprint.social_badges.each_pair do |badge, details|
      %div{id: "#{badge.to_s}_badge", class: 'social_badge'}
        = link_to details[:profile] do
          %img{src: "#{details[:logo]}"}